{
  "name": "prisma-build",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
